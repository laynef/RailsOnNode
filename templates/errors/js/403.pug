<!DOCTYPE html>
html(lang="en")
    head
        include ../utils/meta.pug
        if environment === 'production'
            link(rel='stylesheet', type='text/css', href=`/assets/dist/pages/errors403.css?id=${hashId}`)
    body
        h1(style='text-align: center;') 403 Error
        h2(style='text-align: center;') Forbidden Page
        include ../utils/noscript.pug
        script(src=`/assets/dist/pages/errors403.js?id=${hashId}`, async)
